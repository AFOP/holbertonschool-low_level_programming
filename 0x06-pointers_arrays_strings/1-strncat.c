#include "main.h"

/**
 * _strncat - concatenates two strings with variable n.
 * @src: pointer of the string one
 * @dest: pointer of the string two
 * @n: variable integer
 * Return: a pointer with the resulting of the string
 */
char *_strncat(char *dest, char *src, int n)
{
	
}
